pip install -r requirements.txt
"/Applications/Python 3.14/Install Certificates.command" 
Esse script instala os certificados raiz usados pelo Python no macOS. É justamente o procedimento recomendado pela documentação oficial do Python.